<UserControl x:Class="MeatCounterfeitDetector.UserInterface.Technologist.Edit.ImageEditingControl"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:edit="clr-namespace:MeatCounterfeitDetector.UserInterface.Technologist.Edit"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance edit:ImageEditingControlVM}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="{StaticResource Style2}" Width ="500" Height="500">
            <Border BorderThickness="1" Margin="10" BorderBrush="#FF000000"
                    Background="{StaticResource Style3}">
                <Image Source="{Binding DisplayedImagePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Stretch="Fill"/>
            </Border>
        </Grid>

        <Grid Grid.Column="1" Background="{StaticResource Style2}">
            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>


                <Expander Grid.Row="0" Header="Title" BorderThickness="1,1,1,0" BorderBrush="{DynamicResource BorderBrush}">
                    <Border Height="100" Background="{DynamicResource SecondaryRegionBrush}"/>
                </Expander>
                
                <Expander Grid.Row="1" Header="Title" BorderThickness="1,1,1,0" BorderBrush="{DynamicResource BorderBrush}">
                    <Border Height="100" Background="{DynamicResource SecondaryRegionBrush}"/>
                </Expander>

            </Grid>
        </Grid>
    </Grid>

</UserControl>